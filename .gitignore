node_modules/
*.log
.DS_Store
# scratch: exported session transcripts and local scratch notes
session-*.md
todo.md
20*-*-*-*-*-handoff*.txt
# maintainer-only actions: GitHub settings, secrets, the signing key
MAINTAINER-TODO.md

# Gradle build state: generated, machine-specific, never worth reviewing
android/.gradle/
android/build/
android/app/build/
android/local.properties

# Signing material is a secret and must never be committed: the key file, the
# keystore that holds it, and the properties CI writes from repository secrets.
android/keystore.properties
*.jks
*.keystore
